from setuptools import setup, find_packages

setup(name='pdfcombiner',
      version='1.0',
      description='Tool to combine pdfs in a directory',
      author='Matt Baker',
      packages=find_packages(include=['pdfcombiner']))
